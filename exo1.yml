openapi: 3.0.3
info:
  title: exo 1 ping pong
  version: 1.0.0
servers:
  - url: 'http://localhost:8000'
paths:
  /ping:
    get:
      responses:
        '200':
          description: pong
          content:
            text/plain:
              schema:
                type: string
                example: pong